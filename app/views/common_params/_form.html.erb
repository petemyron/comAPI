<%= form_for(@common_param) do |f| %>
  <% if @common_param.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@common_param.errors.count, "error") %> prohibited this common_param from being saved:</h2>

      <ul>
      <% @common_param.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
